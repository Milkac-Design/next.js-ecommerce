exports.up = async (sql) => {
  await sql`
	CREATE TABLE products(
		id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
		name varchar(50),
		price integer,
		photo_one varchar(50),
		photo_two varchar(50)
		);
		`;
};

exports.down = async (sql) => {
  await sql`
	DROP TABLE IF EXISTS products;
	`;
};
